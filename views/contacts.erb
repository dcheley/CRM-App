<!DOCTYPE html>
<html>
  <head>
    <title>Contact List</title>
  </head>

  <body>
    <h1>Contact List</h1>
    <ul>
      <% Contact.all.each do |contact| %>
      <li><%= contact.first_name %> <%= contact.last_name %></li>
      <% end %>
    </ul>

  </body>
</html>
